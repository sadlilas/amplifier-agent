---
bundle:
  name: amplifier-agent-builtin
  version: 1.0.0
  description: >
    Sealed built-in bundle for the amplifier-agent CLI (per D4 design decision).
    This bundle is vendored inside the amplifier-agent-lib package and loaded via
    amplifier_foundation.load_bundle(). The prepared result is cached to
    $XDG_CACHE_HOME/amplifier-agent/prepared/<version>/ to avoid re-resolution
    on every process start. Do not edit this file outside of a deliberate design change.

includes:
  - bundle: build-up-foundation
    source: git+https://github.com/microsoft/amplifier-foundation@main

session:
  orchestrator:
    module: loop-streaming
    source: git+https://github.com/microsoft/amplifier-module-loop-streaming@main

  context:
    module: context-simple
    source: git+https://github.com/microsoft/amplifier-module-context-simple@main
    config:
      max_tokens: 200000
      compact_threshold: 0.8
      auto_compact: true
---

# amplifier-agent Built-in Bundle

This bundle defines the sealed runtime environment for the **amplifier-agent CLI**.

## Runtime Context

The agent runs inside the `amplifier-agent` CLI process. Approval flows and display
updates are mediated by the **host adapter** — the component that bridges agent-side
events (tool calls, approval requests, stream chunks) to the host application (e.g.
the Paperclip VS Code extension or any compliant JSON-RPC client).

The host adapter is responsible for:

- Routing incoming tool-call requests to the appropriate approval channel.
- Streaming partial assistant turns back to the display layer.

Note: session-transcript persistence (writing `context-messages.jsonl` to
`$XDG_STATE_HOME/amplifier-agent/sessions/<session-id>/`) is a planned
CLI-layer hook responsibility — modeled on `amplifier-app-cli`'s
`IncrementalSaveHook`. It is **not implemented in this build**. The context
module in this bundle (`context-simple`) does not write transcripts; it only
manages in-session message buffering and compaction.

## Bundle Stability

This bundle is **sealed**: its content defines a known, tested agent configuration.
Changing it changes the cache key and invalidates all cached prepared bundles.
Any change must be intentional and reviewed as a design decision.
